{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User account information and role assignments",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Unique user ID matching auth.uid"
        },
        "email": {
          "type": "string",
          "description": "User email address"
        },
        "displayName": {
          "type": "string",
          "description": "Display name or handle"
        },
        "role": {
          "type": "string",
          "enum": ["owner", "admin", "user"],
          "description": "User access level"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of creation"
        }
      },
      "required": ["uid", "email", "role"]
    },
    "GameSettings": {
      "title": "Game Settings",
      "description": "Global permissions and configuration for arcade games",
      "type": "object",
      "properties": {
        "allowGuestPlaying": {
          "type": "boolean",
          "description": "Whether non-admin guests are permitted to play"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last update"
        },
        "updatedBy": {
          "type": "string",
          "description": "UID of owner/admin who updated the setting"
        }
      },
      "required": ["allowGuestPlaying"]
    },
    "GameStartEvent": {
      "title": "Game Start Event",
      "description": "Log record tracking whenever a player launches or plays a game",
      "type": "object",
      "properties": {
        "gameId": {
          "type": "string",
          "description": "Identifier of the game that was started"
        },
        "gameTitle": {
          "type": "string",
          "description": "Human-readable title of the game"
        },
        "userId": {
          "type": "string",
          "description": "Firebase Auth UID or anonymous client ID"
        },
        "userName": {
          "type": "string",
          "description": "Player name, display name or email"
        },
        "userEmail": {
          "type": "string",
          "description": "Email address of authenticated player, if signed in"
        },
        "isGuest": {
          "type": "boolean",
          "description": "Whether the user started the game as an unauthenticated guest"
        },
        "startedAt": {
          "type": "string",
          "description": "ISO timestamp when the game was triggered"
        },
        "device": {
          "type": "string",
          "description": "Device category (desktop, mobile, tablet)"
        }
      },
      "required": ["gameId", "gameTitle", "userId", "userName", "startedAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles and assigned roles"
    },
    "/settings/games": {
      "schema": "GameSettings",
      "description": "System-wide game access rules controlled by the owner"
    },
    "/gameEvents/{eventId}": {
      "schema": "GameStartEvent",
      "description": "Audit log of all game start events tracking which player launched each game"
    }
  }
}
